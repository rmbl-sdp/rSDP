% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdp_dates.R
\name{sdp_get_dates}
\alias{sdp_get_dates}
\title{Discover available dates for an SDP time-series product.}
\usage{
sdp_get_dates(catalog_id, source = c("auto", "stac", "manifest"))
}
\arguments{
\item{catalog_id}{Character. A valid SDP catalog ID.}

\item{source}{One of \code{"auto"} (default), \code{"stac"}, or \code{"manifest"}.
\code{"auto"} tries STAC first, falls back to the manifest.
\code{"stac"} queries the live STAC catalog (requires network).
\code{"manifest"} uses only the baked offline manifest.}
}
\value{
A sorted \code{Date} vector of available dates.
}
\description{
Returns a sorted vector of dates for which data is available. For
regular products (Yearly, Monthly, Daily), dates are computed from the
catalog's MinDate/MaxDate. For irregular products (Weekly drone
imagery), dates are discovered from the STAC catalog (online) or a
baked manifest (offline).
}
\examples{
\dontrun{
## Discover available dates for weekly drone imagery
dates <- sdp_get_dates("R6D001")
head(dates)

## Works offline with baked manifest
dates <- sdp_get_dates("R6D001", source = "manifest")

## Regular products compute from catalog range
dates <- sdp_get_dates("R4D001")  # Yearly snow persistence
}
}
